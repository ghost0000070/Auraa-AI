# =====================================================
# Auraa-AI Environment Configuration
# Copy this file to .env and fill in your values
# =====================================================

# Supabase Configuration (Required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Owner Configuration (Required for admin features)
VITE_OWNER_EMAIL=your_email@example.com
VITE_OWNER_UID=your_supabase_user_id

# Optional: Performance Monitoring
VITE_ENABLE_ANALYTICS=true

# Optional: Rate Limiting
VITE_API_RATE_LIMIT=100

# Optional: Error Tracking (Sentry)
# VITE_SENTRY_DSN=your_sentry_dsn
# VITE_SENTRY_ENVIRONMENT=production

# =====================================================
# Polar.sh Configuration (Required for subscriptions)
# =====================================================
VITE_POLAR_ACCESS_TOKEN=your_polar_access_token_here

# Product IDs from your Polar.sh dashboard
# VITE_POLAR_PRO_PRODUCT_ID=your_pro_product_id
# VITE_POLAR_ENTERPRISE_PRODUCT_ID=your_enterprise_product_id

# =====================================================
# Edge Function Environment Variables
# Set these in Supabase Dashboard > Edge Functions > Secrets
# =====================================================

# Supabase (auto-provided by Supabase, but may need to set explicitly)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your_supabase_anon_key
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Polar.sh Configuration for Edge Functions
# POLAR_ACCESS_TOKEN=your_polar_access_token_here
# POLAR_WEBHOOK_SECRET=your_polar_webhook_secret_here
# POLAR_PRO_PRODUCT_ID=your_pro_product_id
# POLAR_ENTERPRISE_PRODUCT_ID=your_enterprise_product_id
# POLAR_ORGANIZATION_ID=your_polar_organization_id

# RSA Keys for Integration Encryption
# Generate with: openssl genrsa -out private.pem 2048 && openssl rsa -in private.pem -pubout -out public.pem
# INTEGRATION_RSA_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
# INTEGRATION_RSA_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----

# Email Service Configuration (for notification emails)
# Supports: resend, sendgrid, smtp
# EMAIL_SERVICE=resend
# EMAIL_API_KEY=your_email_api_key_here
# EMAIL_FROM_ADDRESS=noreply@yourdomain.com
# EMAIL_FROM_NAME=Auraa AI

# AI/LLM Configuration (for AI features)
# OPENAI_API_KEY=your_openai_api_key
# ANTHROPIC_API_KEY=your_anthropic_api_key

# =====================================================
# Agent Worker Environment Variables
# Required when running: npm run worker
# These are NOT VITE_ prefixed (server-side only)
# =====================================================

# Supabase Service Role Key (full database access - never expose to browser)
# Get from: Supabase Dashboard > Settings > API > service_role key
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# RSA Private Key for decrypting integration credentials
# Generate keypair: openssl genrsa -out private.pem 2048 && openssl rsa -in private.pem -pubout -out public.pem
# Copy entire PEM including -----BEGIN/END----- markers
# For multi-line in .env, use \n or load from file
INTEGRATION_RSA_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----

# =====================================================
# Development Only
# =====================================================
# VITE_DEV_MODE=true
# VITE_MOCK_AUTH=false
