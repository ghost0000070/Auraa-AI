# Stripe Configuration Template
# IMPORTANT: This is a template file with placeholder values only.
# Copy this to .env.stripe and replace with your actual Stripe keys.
# Never commit real API keys to version control!

# Stripe API Keys (Get from https://dashboard.stripe.com/apikeys)
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_KEY
# STRIPE_SECRET_KEY should be stored in Firebase Secrets Manager, not in .env files
# Run: firebase functions:secrets:set STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_WITH_YOUR_SECRET

# Firebase Cloud Functions URL
FUNCTIONS_BASE_URL=https://us-central1-your-project.cloudfunctions.net

# Test Connected Account
TEST_CONNECTED_ACCOUNT_ID=acct_test_your_account_id

# Test Price ID (created via createSubscriptionProductEndpoint)
TEST_PRICE_ID=price_test_your_price_id

# Application URLs
CHECKOUT_URL=https://your-project.web.app/checkout-embedded.html
RETURN_URL=https://your-project.web.app/checkout-return.html

# Firebase Project
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_REGION=us-central1

# Subscription Configuration
DEFAULT_SUBSCRIPTION_AMOUNT=2999  # $29.99
DEFAULT_CURRENCY=usd
DEFAULT_INTERVAL=month

# Notes:
# - Never commit actual secret values to version control
# - Use Firebase Secrets Manager for production: firebase functions:secrets:set STRIPE_SECRET_KEY
# - Test mode keys start with pk_test_ and sk_test_
# - Live mode keys start with pk_live_ and sk_live_
