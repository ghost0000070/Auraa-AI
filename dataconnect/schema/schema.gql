type AgentTask @collection(name: "agent_tasks") {
  id: ID!
  prompt: String!
  status: String!
  result: String
}

type AgentAction @collection(name: "agent_actions") {
  id: ID!
  actionType: String!
  parameters: JSON
  result: JSON
  status: String!
}